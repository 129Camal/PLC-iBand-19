extends layout

block content
    .card
        .card-header
            h2
                b Login
        .card-body
            include messages.pug
            form(action="/users/login", method="post")
                .form-group
                    label Email:
                    input.form-control(type="text", name="email", placeholder="Email address" required)
                .form-group
                    label Password: 
                    input.form-control(type="password", name="password", placeholder="Enter Password" required)
                button.btn-outline-secondary.btn-sm(type="submit") Submit
                br
                br
                a(href="http://localhost:4020/users/register") Don´t have an account? Click here to register!
